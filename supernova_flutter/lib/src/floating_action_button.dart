import 'package:flutter/material.dart';

const floatingActionButtonSize = 56;

/// Additional bottom padding for a scrollable widget to ensure a
/// [FloatingActionButton] in front of it doesn't hide any content.
const scrollPaddingForFab =
    floatingActionButtonSize + kFloatingActionButtonMargin;
